from apollo.agents.base_agent import AgentBase


class MediaAgent(AgentBase):
    agent_id = "agent_media"
    name = "Media Agent"
    role = "Plans and coordinates image, video, audio, and brand assets."

